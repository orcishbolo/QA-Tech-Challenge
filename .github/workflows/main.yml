name: QA Tech Challenge
on: [push]
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.5
      - name: Cypress.io
        uses: cypress-io/github-action@v6.5.0
        with:
          spec: e2e/scripts/*.cy.js
